import 'package:flutter/material.dart';
import 'Temp//temps.dart';

class App extends StatelessWidget{
  Widget build (context){
    return MaterialApp(
      title: 'Log In',
      home: Scaffold(
        body:TempApp(),
        ),
    );
  }
}
